//区间加和问题，让你对一段区间操作，然后有若干个query，每个query询问每个区间的加和
#include <bits/stdc++.h>
using namespace std;
using ll=long long;
const int MAXN=5e4+10;
void solve()
{
  vector<ll>a(MAXN),lazy(a),s(a);
  vector<int>id(MAXN);
  
}
int main()
{
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  #ifdef LOCAL
  freopen("/Users/xiangyanxin/code/Algorithom/in.txt","r",stdin);
  freopen("/Users/xiangyanxin/code/Algorithom/out.txt","w",stdout);
  #endif
  return 0;
}